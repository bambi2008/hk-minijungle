# FiveCrop 手机纠错与可靠病例（2026-08-31）

## 手机怎么用

现有 TestFlight 0.1.0 (1) 加载线上 FiveCrop 页面，本次功能不要求重装。完全关闭后重开，或在 App 顶部下拉刷新。

1. 选择植物，拍实物照片，等待真实识别。
2. 结果下点“判斷不對？告訴我們”，选择你的判断，补充土壤、光照等依据。
3. 选择复查时间，点“保存糾錯和照片”。只有看到“已保存到私有雲端”才表示云端保存成功。
4. 在“我的複查”中打开病例，记录改善／无变化／变差、实际采取的措施，可补拍同角度照片。
5. 所有纠错先待核实。用户猜测和症状改善都不自动等于病因确诊。

提醒是 App 内待复查提示，不是关闭 App 后的推送。断网时待同步数据保留在 IndexedDB；重新联网会重试，也可点“重新同步”。上限20条待同步。不要在同步前清除 App 数据。

此测试阶段使用装置专属随机凭证，无账户登录或跨设备恢复；重装／清除网站数据会失去本机病例访问凭证。云端记录仍保留，人工审核端可查看。清除本机数据不等于删除云端数据。

## 隐私和学习边界

- 图片先经浏览器画布压缩重编码，去除 EXIF；提交纠错时保存的是与视觉分析签名凭证绑定的压缩照片。
- 病例保存在 Neon，照片保存在 private Vercel Blob。只能凭所属装置凭证或人工审核凭证读取，不使用公开图片链接。SHA-256/大小在读取时再验证。
- 保存纠错的同意与模型改进的同意分开，后者默认不勾选。可撤回；撤回立即排除后续候选导出。已经训练发布的模型不能通过删除原数据逆向撤销学习。
- “我的複查”可永久删除整例与私有照片；删除前页面二次确认（兼容已有 WKWebView，无需原生弹窗）。清理失败会停止该病例使用并提示重试。
- 只有：用户自愿同意 + 完整原照 + 至少一次附照片复查 + 独立人工核实 + 非测试样本，才可进入训练候选。
- 原诊断、模型标识与观察、用户假设、环境、每次实际操作／结果、人工审核和授权撤回分别记录，不覆盖原始判断。
- 新增复查证据会使旧审核失效；审核提交校验所读证据摘要，防止审核旧版本。
- 照片签名证明“这张照片对应这次服务器分析”，不证明照片来自真实植物、不证明病因正确；仍需人工辨别合成、翻拍、重复及不可靠标签。

## 人工核实

打开 https://fivecrop-testflight.vercel.app/case-review.html ，输入审核凭证。凭证仅留在该页面内存，不写浏览器存储或 URL。用完点“锁定”。

本机项目已保存忽略 Git 的 `.env.feedback.secrets.local`。在项目根目录运行 `npm run learning:review-key` 可将审核凭证复制到 macOS 剪贴板；贴入审核页后清空剪贴板。不要发到聊天、截图或 Git。凭证不是阿里云 key。

核实时同时查看原照、视觉观察、环境及每次复查。写明支持与排除原因的证据，不能仅重复用户猜测。若信息不足，保持待核实／未采纳。此版本不把纠错自动注入共享病理库或大模型参数，避免错误标签污染。

`GET /api/learning/admin/export` 返回已授权且通过审核的候选清单与受保护照片读取地址，分页使用 `before=nextCursor`，需审核凭证。不是直接可上传训练的最终数据集；正式训练前仍须做授权复核、人工去标识、图像去重和分组拆分。

## 以后训练哪个视觉模型

首轮候选：**Qwen3-VL-8B-Instruct + SFT 高效训练（LoRA）**。已有 Qwen/DashScope 接入方便比较与替换；不表示微调8B一定优于现用 qwen-vl-plus。百炼官方当前支持该型号的全参 SFT 和 efficient_sft，不支持其 DPO。

建议先积累约1,000例及以上独立、已授权、人工核实且多样的病例，再进行小规模试训；这是项目规划起点，不是准确率保证或硬门槛。五种作物都要覆盖健康样本、缺光／缺水易混淆对、环境差异和不确定样本。相同植物的多次复查不能当成独立病例数量。

先按植物／装置分组，再做完全相同和近重复照片去重，把同一组留在同一训练／验证／测试分区。固定未见过的真机照片作为盲测，比较作物识别、病因混淆、拒绝不确定判断的能力、行动安全性、延迟和费用，优于当前模型才切换。不要只报告总体准确率。

输入应包含照片和在诊断当时可获得的环境信息；输出是人工整理的可见征象、可能病因、排除原因、需补充的信息、保守动作和复查时间。不能把未来复查结果偷偷放入初诊输入，造成评测泄漏。

官方资料（2026-08-31 核对）：
- https://help.aliyun.com/zh/model-studio/fine-tuning-api-guide （支持模型／训练方式；多模态 SFT 使用 data.jsonl + 图片的 ZIP）
- https://help.aliyun.com/en/model-studio/model-training-overview

本次没有启动训练任务、购买训练资源或宣称模型已因反馈而学会。

## 运维与验证

生产服务：https://fivecrop-testflight.vercel.app

新增专用环境变量：DATABASE_URL、DATABASE_URL_UNPOOLED、BLOB_READ_WRITE_TOKEN、FIVECROP_LEARNING_SECRET、FIVECROP_REVIEW_TOKEN。只在服务器使用。真实视觉沿用 DASHSCOPE_API_KEY / qwen-vl-plus。

- `npm run learning:test`：隔离单测及HTTP权限、输入边界测试。
- `npm run check`：既有项目回归测试。
- `node --env-file=.env.feedback.production.local node_modules/drizzle-kit/bin.cjs migrate --config=drizzle.config.mjs`：应用已审阅的专用数据库迁移。
- `node --env-file=.env.feedback.secrets.local scripts/learning-cloud-smoke.mjs https://fivecrop-testflight.vercel.app --real-vision`：一次真实模型请求，创建专用测试病例、照片、复查、审核、撤回及删除。测试标识排除训练；只删除本次脚本创建的UUID。
- `scripts/learning-ui-server.mjs`：仅本机8015测试页面，真实视觉 + 纠错组件人工操作测试，不包含在公开构建中。

TestFlight阶段写入限额：每设备每天30例／100个复查事件，全项目每天150例／750个复查事件；照片上限2MB。遇到限额会明确报错并保留本机队列，不转存临时目录。该限额不是计费预算封顶。Neon已选择free_v3；Blob按当前项目套餐使用。

生产依赖安全检查通过。数据库迁移工具仅用于开发，不打包进公开静态文件。发布时只白名单复制客户端文件，环境文件、服务端源码、SQLite数据均不公开。
